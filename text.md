Optimized tool selection

Deep architectural audit of Angular project

Searched for regex `constructor\s*\(` (`**/src/app/**/*.ts`), 7 results

Searched for regex `\.subscribe\(` (`**/src/app/**/*.ts`), 9 results

Searched for regex `\*ngIf|\*ngFor` (`**/src/app/**/*.html`), 19 results

Searched for regex `px|[^:]\s[0-9]+px` (`**/src/app/**/*.scss`), 12 results

Searched for regex `#[0-9A-Fa-f]{3,6}|rgba|rgb\(` (`**/src/app/**/*.scss`), 38 results

Searched for regex `get\s+\w+\s*\(|get\s+\w+.*\{` (`**/src/app/components/**/*.ts`), 2 results

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/components/molecules/navbar/navbar.component.ts#1-1), lines 1 to 50

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/components/molecules/navbar/navbar.component.html)

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/features/public/pages/product-detail-page/product-detail-page.component.ts#1-1), lines 1 to 100

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/components/pages/contact/contact.component.ts#1-1), lines 1 to 50

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/components/atoms/button/button.component.ts)

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/components/atoms/button/button.component.html)

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/components/pages/contact/contact.component.html)

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/features/admin/pages/product-form/product-form.component.ts)

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/components/templates/categories-template/categories-template.component.ts#1-1), lines 1 to 50

Searching for regex `:\s*\d+px(?!\s*solid)|(?<!border-radius:.*)\s\d+px(?!\s*solid)|padding|margin|width|height|font-size|gap|top|left|right|bottom` (`**/src/app/components/**/*.scss`)

Searching for regex `:\s*\d+px(?!\s*solid)|(?<!border-radius:.*)\s\d+px(?!\s*solid)|padding|margin|width|height|font-size|gap|top|left|right|bottom` (`**/src/app/features/**/*.scss`)

Searched for regex `import.*from.*@angular|imports:\s*\[` (`**/src/app/**/*.ts`), 50 results

Searched for regex `padding:\s*\d|margin:\s*\d|width:\s*\d|height:\s*\d|font-size:\s*\d|gap:\s*\d|top:\s*\d|left:\s*\d|right:\s*\d|bottom:\s*\d` (`**/src/app/**/*.scss`), 100 results

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/components/molecules/footer/footer.component.scss#1-1), lines 1 to 100

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/components/organisms/hero/hero.component.scss)

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/components/atoms/button/button.component.scss)

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/assets/tokens/colors.scss)

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/assets/tokens/margins.scss)

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/features/public/pages/catalog-page/catalog-page.component.ts#1-1), lines 1 to 100

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/features/admin/pages/categories-manager/categories-manager.component.ts)

Searched for regex `@if|@for` (`**/src/app/**/*.ts`), no results

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/features/public/pages/product-detail-page/product-detail-page.component.html#40-40), lines 40 to 50

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/components/templates/categories-template/categories-template.component.html)

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/features/admin/pages/categories-manager/categories-manager.component.html)

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/features/public/pages/product-detail-page/product-detail-page.component.html#1-1), lines 1 to 60

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/components/molecules)

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/components/atoms/inputs)

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/app.module.ts)

Searched for regex `export class` (`**/src/app/components/atoms/**/*.ts`), 2 results

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/services/contact.service.ts)

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/core/services/product.service.ts#1-1), lines 1 to 60

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/core/services/category.service.ts)

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/components/atoms/atoms.module.ts)

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/components/molecules/molecules.module.ts)

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/components/templates/templates.module.ts)

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/components/organisms/organisms.module.ts)

Searched for regex `disabled.*async|class.*async|\[.*\].*async` (`**/src/app/features/admin/pages/**/*.html`), 1 result

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/components/templates/categories-template/categories-template.component.ts#40-40), lines 40 to 57

Read [](file:///c%3A/Users/SharomB/Desktop/tecsisman_web/src/app/features/admin/pages/product-form/product-form.component.html)

Created memory file [](file:///c%3A/Users/SharomB/AppData/Roaming/Code/User/workspaceStorage/a557c66b4efed74cacf27e69fc765766/GitHub.copilot-chat/memory-tool/memories/MGI5MmQ2Y2UtYjY2My00ZmNkLWJiN2ItNTFjMWUwZTZkZDg3/angular-architecture-analysis.md)

Read [](file:///c%3A/Users/SharomB/AppData/Roaming/Code/User/workspaceStorage/a557c66b4efed74cacf27e69fc765766/GitHub.copilot-chat/chat-session-resources/0b92d6ce-b663-4fcd-bb7b-51c1e0e6dd87/call_53237403__vscode-1778196235411/content.txt)

# **COMPREHENSIVE ANGULAR ARCHITECTURE ANALYSIS**
## Staff-Level Code Review: Clean Architecture & RxJS Violations

---

## **1. ANGULAR ARCHITECTURE VIOLATIONS**

### **1.1 Obsolete Constructor Pattern (Should Use `inject()`)**

**Severity**: HIGH | **Impact**: Dependency Injection Testability

Found **7 instances** using class constructors instead of modern `inject()` API:

| File | Line | Issue |
|------|------|-------|
| product-detail-page.component.ts | 68 | Constructor with multiple deps |
| category.service.ts | 10 | Constructor injection |
| catalog-page.component.ts | 94 | Constructor injection |
| product-form.component.ts | 32 | Constructor with deps |
| categories-manager.component.ts | 36 | Constructor with deps |
| contact.component.ts | 24 | Constructor injection |
| categories-template.component.ts | 22 | Constructor injection |

**Current Code** product-detail-page.component.ts:
```typescript
export class ProductDetailPageComponent implements OnInit {
  constructor(
    private readonly route: ActivatedRoute,
    private readonly productService: ProductService,
    private readonly fb: FormBuilder,
    private readonly destroyRef: DestroyRef,
  ) {}

  ngOnInit(): void {
    this.product$.pipe(takeUntilDestroyed(this.destroyRef)).subscribe();
  }
}
```

**Refactored Code**:
```typescript
import { inject } from '@angular/core';

export class ProductDetailPageComponent implements OnInit {
  private readonly route = inject(ActivatedRoute);
  private readonly productService = inject(ProductService);
  private readonly fb = inject(FormBuilder);
  private readonly destroyRef = inject(DestroyRef);

  ngOnInit(): void {
    this.product$.pipe(takeUntilDestroyed(this.destroyRef)).subscribe();
  }
}
```

---

### **1.2 Redundant NgModule Wrappers Around Standalone Components**

**Severity**: HIGH | **Impact**: Unnecessary Complexity, Bundle Size

**Files Affected**:
- atoms.module.ts - wraps `ButtonComponent`
- molecules.module.ts - wraps `NavbarComponent`, `FooterComponent`
- templates.module.ts - wraps 5 template components
- organisms.module.ts - wraps 5 organism components
- app.module.ts - root module still using old decorator

**Current Code** atoms.module.ts:
```typescript
@NgModule({
    imports: [CommonModule, ButtonComponent],
    exports: [ButtonComponent]
})
export class AtomsModule { }
```

**Issue**: Standalone components shouldn't be wrapped in modules. This adds unnecessary indirection.

**Refactored Approach**:
1. **Remove all module wrappers** - import standalone components directly in features
2. **Update app.module.ts**:

```typescript
import { NgModule } from '@angular/core';
import { BrowserModule, provideClientHydration } from '@angular/platform-browser';
import { AppComponent } from './app.component';
import { NavbarComponent } from './components/molecules/navbar/navbar.component';
import { FooterComponent } from './components/molecules/footer/footer.component';

@NgModule({
  declarations: [AppComponent],
  imports: [
    BrowserModule,
    NavbarComponent,  // Direct import instead of MoleculesModule
    FooterComponent,
  ],
  providers: [provideClientHydration()],
  bootstrap: [AppComponent],
})
export class AppModule {}
```

3. **Update imports in standalone components** - directly import needed components instead of modules

---

### **1.3 Heavy/Redundant Imports in Standalone Components**

**Severity**: MEDIUM | **Impact**: Performance, Change Detection

**Example** product-detail-page.component.ts:
```typescript
imports: [
    NgIf,       // ❌ Redundant - using *ngIf in template
    NgFor,      // ❌ Redundant - using *ngFor in template
    FormsModule,
    ReactiveFormsModule,
    AsyncPipe,
    DecimalPipe,  // ❌ Redundant - not in template
    CurrencyPipe, // Used in template
    DatePipe,     // Used in template
],
```

**Refactored**:
```typescript
imports: [
    FormsModule,
    ReactiveFormsModule,
    AsyncPipe,
    CurrencyPipe, // Only used pipes
    DatePipe,
    // REMOVE NgIf, NgFor - migrate to @if/@for control flow
]
```

---

## **2. STATE MANAGEMENT & RXJS VIOLATIONS**

### **2.1 Manual `.subscribe()` Calls Without Error Handlers**

**Severity**: CRITICAL | **Impact**: Unhandled Errors, Memory Leaks

**Found 9+ instances** of `.subscribe()` without `error` or `complete` handlers:

| File | Line | Pattern | Issue |
|------|------|---------|-------|
| product-detail-page.component.ts | 76 | `.subscribe()` | Empty subscription, dead code |
| product-detail-page.component.ts | 89 | `.subscribe(() => {})` | Missing error handler |
| product-form.component.ts | 61 | `.subscribe(() => {})` | Missing error/complete |
| categories-manager.component.ts | 47 | `.subscribe(() => {})` | No error handling |
| categories-manager.component.ts | 53 | `.subscribe()` | Completely empty |
| categories-template.component.ts | 33,40,48 | `.subscribe()` | Triple violation |
| contact.component.ts | 40 | `.subscribe({})` | Incomplete handler |

**Current Code** product-detail-page.component.ts:
```typescript
ngOnInit(): void {
  this.product$.pipe(takeUntilDestroyed(this.destroyRef)).subscribe(); // ❌ Empty!
}
```

**Current Code** product-detail-page.component.ts:
```typescript
submitComment(product: Product): void {
  this.productService.addComment(product.id, this.commentForm.getRawValue()).subscribe(() => {
    // ❌ No error handler! Network errors silently fail
    this.commentForm.reset({ author: '', rating: 5, message: '' });
  });
}
```

**Refactored Code**:
```typescript
submitComment(product: Product): void {
  if (this.commentForm.invalid) {
    this.commentForm.markAllAsTouched();
    return;
  }

  this.productService
    .addComment(product.id, this.commentForm.getRawValue())
    .pipe(takeUntilDestroyed(this.destroyRef))
    .subscribe({
      next: () => {
        this.commentForm.reset({ author: '', rating: 5, message: '' });
      },
      error: (err) => {
        console.error('Failed to add comment:', err);
        this.errorMessage = 'No pudimos guardar tu comentario. Intenta más tarde.';
      }
    });
}
```

**For empty subscriptions** product-detail-page.component.ts:
```typescript
// ❌ Current - pointless subscription
ngOnInit(): void {
  this.product$.pipe(takeUntilDestroyed(this.destroyRef)).subscribe();
}

// ✅ Refactored - use AsyncPipe in template instead
// Template: <span>{{ (product$ | async)?.name }}</span>
// OR side effect approach:
ngOnInit(): void {
  this.product$
    .pipe(
      tap(() => this.selectedImageIndex = 0),
      takeUntilDestroyed(this.destroyRef)
    )
    .subscribe();
}
```

**Critical Pattern Fix** categories-template.component.ts:
```typescript
// ❌ Current - 3 separate subscribes
createCategory(newCategory: { name: string }): void {
  this.categoriesService.createCategory(newCategory.name).subscribe(createdCategory => {
    this.newCategory = { name: '' };
    this.refreshCategories(); // Calls refreshSubject.next() which triggers another subscribe below
  });
}

updateCategory(updatedCategory: Category): void {
  if (updatedCategory.id) {
    this.categoriesService.updateCategory(updatedCategory.id, updatedCategory).subscribe(() => {
      this.refreshCategories();
    });
  }
}

deleteCategory(id: string): void {
  this.categoriesService.deleteCategory(id).subscribe(() => {
    this.refreshCategories();
  });
}

// ✅ Refactored - Use subjects and proper RxJS composition
private readonly actionSubject = new Subject<() => Observable<void>>();

readonly categories$ = merge(
  this.refreshSubject,
  this.actionSubject.pipe(
    switchMap(action => action()),
  )
).pipe(
  switchMap(() => this.categoriesService.getAllCategories()),
  shareReplay({ bufferSize: 1, refCount: true }),
);

createCategory(newCategory: { name: string }): void {
  this.actionSubject.next(() =>
    this.categoriesService.createCategory(newCategory.name).pipe(
      tap(() => this.newCategory = { name: '' })
    )
  );
}

updateCategory(updatedCategory: Category): void {
  if (updatedCategory.id) {
    this.actionSubject.next(() =>
      this.categoriesService.updateCategory(updatedCategory.id, updatedCategory)
    );
  }
}

deleteCategory(id: string): void {
  this.actionSubject.next(() =>
    this.categoriesService.deleteCategory(id)
  );
}
```

---

### **2.2 Getters Rendered Directly in HTML (Change Detection Antipattern)**

**Severity**: HIGH | **Impact**: Excessive Change Detection Cycles

**Found 2 instances** where getters are called in templates:

| File | Line | Getter | Usage |
|------|------|--------|-------|
| button.component.ts | 17 | `get classes()` | button.component.html - `[class]="classes"` |
| contact.component.ts | 28 | `get f()` | contact.component.html - `f.name.errors`, `f.email.errors`, etc. |

**Current Code** button.component.ts:
```typescript
export class ButtonComponent {
  @Input() variant: 'primary' | 'outline' | 'light' | 'dark' = 'primary';
  @Input() size: 'sm' | 'md' | 'lg' = 'md';
  @Input() block: boolean = false;

  get classes(): string {  // ❌ Called on every change detection
    const base: string[] = ['app-btn'];
    switch (this.variant) { /* ... */ }
    switch (this.size) { /* ... */ }
    if (this.block) base.push('app-btn--block');
    return base.join(' ');
  }
}
```

**Template** button.component.html:
```html
<button [class]="classes" [attr.type]="type" [disabled]="disabled">
```

**Refactored Code**:
```typescript
import { ChangeDetectionStrategy, Component, Input, computed } from '@angular/core';

@Component({
  selector: 'app-button',
  templateUrl: './button.component.html',
  styleUrls: ['./button.component.scss'],
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class ButtonComponent {
  @Input() type: 'button' | 'submit' | 'reset' = 'button';
  @Input() customStyle: string = '';
  @Input() variant: 'primary' | 'outline' | 'light' | 'dark' = 'primary';
  @Input() size: 'sm' | 'md' | 'lg' = 'md';
  @Input() block: boolean = false;
  @Input() disabled: boolean = false;

  // ✅ Use computed signal for memoization
  readonly classes = computed(() => {
    const base: string[] = ['app-btn'];
    switch (this.variant()) {
      case 'primary': base.push('app-btn--primary'); break;
      case 'outline': base.push('app-btn--outline'); break;
      case 'light': base.push('app-btn--light'); break;
      case 'dark': base.push('app-btn--dark'); break;
    }
    switch (this.size()) {
      case 'lg': base.push('app-btn--lg'); break;
      case 'sm': base.push('app-btn--sm'); break;
    }
    if (this.block()) base.push('app-btn--block');
    if (this.customStyle()) base.push(this.customStyle());
    return base.join(' ');
  });
}
```

**Template Update**:
```html
<button [class]="classes()" [attr.type]="type" [disabled]="disabled">
  <ng-content></ng-content>
</button>
```

---

### **2.3 Direct AsyncPipe in Change Detection Conditions**

**Severity**: MEDIUM | **Impact**: Potential Memory/Performance Issues

**Found in** categories-manager.component.html:
```html
<button ... [disabled]="(rows$ | async)?.length === 1">
```

**Issue**: This creates a new subscription each time the template is evaluated, causing duplicate async operations.

**Refactored**:
```typescript
// In component
readonly isLastCategory$ = combineLatest([this.rows$, this.rows$]).pipe(
  map(([rows]) => rows.length === 1),
  shareReplay({ bufferSize: 1, refCount: true })
);
```

```html
<!-- In template -->
<button ... [disabled]="isLastCategory$ | async">
```

---

## **3. UI & ATOMIC DESIGN VIOLATIONS**

### **3.1 Hardcoded Pixel Values (Should Use SCSS Tokens)**

**Severity**: HIGH | **Impact**: Design Inconsistency, Maintenance

**Found 50+ instances** of hardcoded `rem`/`px` values:

| Component | File | Line | Violation |
|-----------|------|------|-----------|
| Button | button.component.scss | 15,22-24 | `padding: 0.625rem 1rem;` |
| Button | button.component.scss | 60 | `border-radius: 1.25rem;` |
| Footer | footer.component.scss | 61-62 | `width: 1.75rem; height: 1.75rem;` |
| Hero | hero.component.scss | 12 | `padding-top: 6rem;` |
| Product Detail | product-detail-page.component.scss | 7,62 | Multiple hardcoded values |

**Create Spacing Tokens** - Add src/assets/tokens/spacing.scss:
```scss
// Spacing Scale (based on 0.25rem base unit)
$spacing-2xs: 0.25rem;    // 4px
$spacing-xs: 0.5rem;      // 8px
$spacing-sm: 0.75rem;     // 12px
$spacing-md: 1rem;        // 16px
$spacing-lg: 1.5rem;      // 24px
$spacing-xl: 2rem;        // 32px
$spacing-2xl: 3rem;       // 48px
$spacing-3xl: 4rem;       // 64px
$spacing-4xl: 6rem;       // 96px

// Dimension Scale
$size-xs: 1.1rem;
$size-sm: 1.25rem;
$size-md: 1.75rem;
$size-lg: 2.25rem;
$size-xl: 3rem;

// Border Radius
$radius-sm: 0.5rem;
$radius-md: 0.75rem;
$radius-lg: 1rem;
$radius-pill: 999px;
```

**Add to** src/assets/tokens/index.scss:
```scss
@forward './colors.scss';
@forward './fonts.scss';
@forward './breakpoints.scss';
@forward './margins.scss';
@forward './shadows.scss';
@forward './spacing.scss';  // ✅ Add this
```

**Refactored Code** button.component.scss:
```scss
@use '../../../../assets/tokens/spacing.scss' as *;
@use '../../../../assets/tokens/colors.scss' as *;
@use '../../../../assets/tokens/fonts.scss' as *;

.app-btn {
  @include inter-button(1rem);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border-radius: $radius-md;        // ✅ Token instead of 0.75rem
  padding: $spacing-sm $spacing-md; // ✅ Token instead of 0.625rem 1rem
  border: 1px solid transparent;
  text-align: center;
  cursor: pointer;

  &--lg {
    padding: $spacing-md $spacing-lg;  // ✅ 0.75rem 1.25rem → token
    @include inter-button(1.05rem);
  }

  &--sm {
    padding: $spacing-xs $spacing-sm;  // ✅ 0.5rem 0.75rem → token
    @include inter-button(0.95rem);
  }

  &--block {
    width: 100%;
  }
}
```

---

### **3.2 Hardcoded Colors (Should Use SCSS Color Tokens)**

**Severity**: HIGH | **Impact**: Brand Inconsistency

**Found in** hero.component.scss:
```scss
background: linear-gradient(180deg, rgba(4,49,76,0.2) 0%, rgba(4,49,76,0.1) 100%);
//                                  ^^^^^^^^^^^^^^ Hardcoded!
```

**Create Color Opacity Tokens** - Update src/assets/tokens/colors.scss:
```scss
// Existing colors
$black-00: #000000;
$blue-01: #04314C;
// ... etc

// ✅ Add opacity variants
$blue-01-opacity-10: rgba($blue-01, 0.1);
$blue-01-opacity-20: rgba($blue-01, 0.2);
$white-00-opacity-04: rgba($white-00, 0.04);
$white-00-opacity-08: rgba($white-00, 0.08);
$white-00-opacity-10: rgba($white-00, 0.1);
$white-00-opacity-12: rgba($white-00, 0.12);
$white-00-opacity-18: rgba($white-00, 0.18);
$white-00-opacity-78: rgba($white-00, 0.78);
$white-00-opacity-90: rgba($white-00, 0.9);
```

**Refactored** hero.component.scss:
```scss
@use '../../../../assets/tokens/colors.scss' as *;

.hero {
  &__overlay {
    background: linear-gradient(
      180deg,
      $blue-01-opacity-20 0%,
      $blue-01-opacity-10 100%
    );
  }
}
```

**Apply throughout** product-detail-page.component.scss:
```scss
// ❌ Before
border: 1px solid rgba($white-00, 0.08);

// ✅ After
border: 1px solid $white-00-opacity-08;
```

---

### **3.3 Business Logic in Visual Components (Atoms/Molecules)**

**Severity**: MEDIUM | **Impact**: Violates Single Responsibility Principle

**Current Issue** product-detail-page.component.ts:
```typescript
// ❌ This is a page component, but method is too simple to be here
getStars(rating: number): number[] {
  return Array.from({ length: Math.round(rating) }, (_, index) => index);
}
```

Used in template:
```html
<span *ngFor="let star of getStars(comment.rating)" aria-hidden="true">★</span>
```

**Move to Pipe** - Create src/app/core/pipes/rating-stars.pipe.ts:
```typescript
import { Pipe, PipeTransform } from '@angular/core';

@Pipe({
  name: 'appRatingStars',
  standalone: true,
})
export class RatingStarsPipe implements PipeTransform {
  transform(rating: number): number[] {
    return Array.from(
      { length: Math.round(rating) },
      (_, index) => index
    );
  }
}
```

**Update Template**:
```html
<!-- ❌ Before -->
<span *ngFor="let star of getStars(comment.rating)">★</span>

<!-- ✅ After -->
<span *ngFor="let star of comment.rating | appRatingStars">★</span>
```

---

## **4. TEMPLATE SYNTAX VIOLATIONS (Old vs New Control Flow)**

**Severity**: MEDIUM | **Impact**: Performance, Code Maintainability

### **4.1 Using `*ngFor` instead of `@for`**

**Found 19+ instances** using old structural directive syntax:

| File | Line | Current | Should Be |
|------|------|---------|-----------|
| navbar.component.html | 16 | `*ngFor` | `@for` |
| product-detail-page.component.html | 9,44,50 | `*ngFor` | `@for` |
| contact.component.html | 9,17,25 | `*ngIf` | `@if` |
| blog.component.html | 10 | `*ngFor` | `@for` |

**Current Code** navbar.component.html:
```html
<li *ngFor="let element of menuElements">
  <a class="..." [routerLink]="element.route">{{ element.title }}</a>
</li>
```

**Refactored Code**:
```html
@for (element of menuElements; track element) {
  <li>
    <a class="..." [routerLink]="element.route">{{ element.title }}</a>
  </li>
}
```

**Current Code** contact.component.html:
```html
<div class="app-feedback" *ngIf="submitted && f.name.errors">
  Ingresa un nombre válido
</div>
```

**Refactored Code**:
```html
@if (submitted && f().name.errors) {
  <div class="app-feedback">
    Ingresa un nombre válido
  </div>
}
```

---

## **5. REDUNDANCY & INCONSISTENCY VIOLATIONS**

### **5.1 Duplicate Service Patterns**

**Severity**: MEDIUM | **Impact**: Confusing API, Maintenance Burden

**Found**:
- `CategoryService` core/services/category.service.ts - wraps ProductService
- `CategoriesService` services/categories.service.ts - separate implementation

**Issue**: Naming inconsistency and duplicate abstractions.

**Refactored Solution**:
```typescript
// ✅ Use single source of truth
export class CategoriesService {
  constructor(private readonly productService: ProductService) {}

  getAllCategories(): Observable<Category[]> {
    return this.productService.getCategories();
  }

  getCategory(id: string): Observable<Category | undefined> {
    return this.productService.getCategories().pipe(
      map(cats => cats.find(c => c.id === id))
    );
  }

  create(name: string): Observable<Category> {
    return this.productService.addCategory(name);
  }

  update(categoryId: string, category: Category): Observable<Category> {
    return this.productService.updateCategory(categoryId, category);
  }

  delete(id: string): Observable<void> {
    return this.productService.deleteCategory(id);
  }
}
```

Remove core/services/category.service.ts entirely.

---

## **PRIORITY ACTION PLAN**

### **Phase 1 (CRITICAL - Week 1)**
- [ ] Replace all constructor patterns with `inject()`
- [ ] Add error handlers to all `.subscribe()` calls
- [ ] Remove empty/dead `.subscribe()` calls
- [ ] Migrate templates from `*ngFor/*ngIf` to `@for/@if`

### **Phase 2 (HIGH - Week 2)**
- [ ] Extract getters to computed signals or pipes
- [ ] Remove NgModule wrappers (atoms, molecules, organisms, templates)
- [ ] Create spacing tokens SCSS file
- [ ] Extract `getStars()` to pipe

### **Phase 3 (MEDIUM - Week 3)**
- [ ] Audit and centralize all color opacity variants
- [ ] Unify CategoryService/CategoriesService
- [ ] Add proper error boundaries in templates
- [ ] Implement centralized error handling service

### **Phase 4 (NICE-TO-HAVE - Week 4)**
- [ ] Implement signals-based state management
- [ ] Add @HostListener change detection strategies
- [ ] Performance profiling and optimization
- [ ] Component composition improvements

---

## **SUMMARY OF FINDINGS**

| Category | Count | Severity |
|----------|-------|----------|
| Constructor Injections | 7 | HIGH |
| `.subscribe()` violations | 9+ | CRITICAL |
| Hardcoded px/rem values | 50+ | HIGH |
| Getters in templates | 2 | HIGH |
| Template syntax (*ngFor/*ngIf) | 19+ | MEDIUM |
| Module wrappers | 4 | HIGH |
| Hardcoded colors | 38+ | HIGH |
| **TOTAL** | **130+** | — |

**Estimated Refactoring Time**: 20-30 hours across 4 phases